/**
 * Gets the status_id for completed projects
 * 
 * @returns {Number} The status_id for completed projects
 */
export function getStatusIdForCompletedProjects() {
	// Had to hard code this... I think
	return 6;
}

/**
 * Gets the status_id for closed bugs
 * 
 * @returns {Number} The status_id for closed bugs
 */
export function getStatusIdForClosedBugs() {
	// Had to hard code this... I think
	return 4;
}

/**
 * Gets the sort_id for status
 * 
 * @returns {Number} The sort_id for status
 */
export function getSortIdForStatus() {
	// Had to hard code this... I think
	return 2;
}